export async function fetchLicenseStatusFromGovAPI(licenseId: string, state: string): Promise<string> {
  console.log("🛂 Fetching license status for:", licenseId, "in state:", state);
  // TODO: Replace this stub with real external API logic
  return "ACTIVE"; // Simulate a default valid license
}
